//! Popover component — Wave 1 stub. To be replaced by the full implementation.
use maud::{html, Markup};

pub fn render() -> Markup {
    html! { p.mui-placeholder { "TODO: popover component" } }
}

pub fn showcase() -> Markup {
    render()
}
