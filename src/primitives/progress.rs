//! Progress component — Wave 1 stub. To be replaced by the full implementation.
use maud::{html, Markup};

pub fn render() -> Markup {
    html! { p.mui-placeholder { "TODO: progress component" } }
}
